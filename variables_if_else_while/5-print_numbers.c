#include <stdio.h>

/**
*main - Entry point
*
*Description: programm affiche toute les lettres de l'alphabet
*en minuscule
*
*Return: Always 0 (Success)
*/
int main(void)
{
int c;
for (c = 0; c <= 9; ++c)
{
printf("%d", c);
}
printf("\n");
return (0);
}
